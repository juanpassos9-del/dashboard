# Dashboard de Mercado Intermercado — Diretiva

## Objetivo
Dashboard em Python (Streamlit) que exibe cotações quasi-tempo-real de ativos de múltiplas classes, correlações intermercados com interpretação automática, notícias relevantes e calendário econômico do dia.

## Fontes de Dados

| Tipo | Fonte | Lib |
|---|---|---|
| Cotações | Yahoo Finance | `yfinance` |
| Notícias | RSS Feeds (CNBC, Reuters, MarketWatch) | `feedparser` |
| Calendário Econômico | Investing.com | `requests` + `beautifulsoup4` |

## Ativos Monitorados

### Commodities
- `CL=F` — Petróleo WTI
- `GC=F` — Ouro
- `^VIX` — VIX

### Moedas
- `6J=F` — Iene Japonês Fut.
- `6L=F` — Real Brasileiro Fut.
- `6M=F` — Peso Mexicano Fut.
- `6E=F` — Euro Fut.
- `DX-Y.NYB` — Índice Dólar (DXY)

### Global
- `^GSPC` — S&P 500
- `^RUT` — Russell 2000
- `EWZ` — ETF Brasil
- `EEM` — ETF Emergentes

### Treasuries
- `ZN=F` — T-Note 10Y Fut.
- `^TNX` — Yield 10 Anos
- `^TYX` — Yield 30 Anos

### ADR Brasil
- `PBR` — Petrobras
- `VALE` — Vale
- `BBD` — Bradesco
- `ITUB` — Itaú
- `BDORY` — Banco do Brasil

## Fluxo de Execução

1. `execution/fetch_quotes.py` busca cotações via yfinance
2. `execution/calc_correlations.py` calcula correlações de 30 dias
3. `execution/fetch_news.py` busca notícias via RSS
4. `execution/fetch_calendar.py` busca calendário econômico
5. `app.py` orquestra tudo via Streamlit, atualiza a cada 60s

## Edge Cases
- **Rate limiting yfinance:** cache de 60s para evitar bloqueio
- **Ativo indisponível:** exibir "N/A" sem quebrar o dashboard
- **Fora do horário de mercado:** exibir último preço de fechamento
- **RSS sem resultados:** exibir mensagem "Sem notícias recentes"
- **Calendário vazio:** exibir "Sem eventos hoje"

## Aprendizados
- **Autenticacao:** O dashboard deve bloquear o acesso antes da sidebar quando nao houver usuario autenticado em `st.session_state`. Usar Supabase Auth para cadastro/login com email e senha; cadastro pede apenas email, senha e celular. Quando houver service role, criar novos usuarios via Auth Admin com `email_confirm=True`; se uma conta antiga falhar login com `Email not confirmed`, tentar confirmar via Auth Admin e repetir o login. Salvar celular em `user_metadata` e, quando a tabela `profiles` existir, fazer `upsert` com `user_id`, `email`, `phone`, `role`, `is_active` e `updated_at`. O primeiro usuario cadastrado recebe `role='admin'`; todos os proximos recebem `role='member'`. Se ja existir conta antes da regra e ainda nao houver admin em `profiles`, o proximo usuario autenticado deve ser promovido para `admin`. Limitar novos cadastros a 1000 usuarios quando a contagem em `profiles` ou Auth Admin estiver disponivel. Nunca salvar senha em tabela propria. O schema recomendado da tabela fica em `supabase_profiles.sql`.
- **Market Report intradiario:** Gerar no maximo 3 leituras por dia (`manha`, `tarde`, `noite`) e salvar em `market_report_daily.json`/`app_state.market_report_daily`. `market_report` continua apontando para o ultimo report para compatibilidade, mas a tela deve ler o historico do dia para preservar as analises ate virar a data local de Sao Paulo.
- **Market Report automatico:** Usar o workflow dedicado `.github/workflows/market_report.yml` para gerar/sincronizar automaticamente as leituras as 07:05, 13:05 e 19:05 de Sao Paulo. O workflow chama `execution/run_market_report_update.py`, que sincroniza `market_report` e `market_report_daily` no Supabase.
- **Market Report IA resiliente:** A geracao do Market Report deve tentar Gemini primeiro e OpenAI como fallback quando houver quota/modelo indisponivel. Antes de gerar, a tela deve preparar `mercados_globais.json` e `dados_mercado.json` a partir de Supabase/cache para o fallback local nao ficar com `---`. Se nenhuma chave externa (`GOOGLE_API_KEY`, `GEMINI_API_KEY`, `OPENAI_API_KEY`) existir no ambiente online, gerar um report local deterministico profissional com dados/noticias disponiveis e provider `Local/sem chave IA`, em vez de deixar a tela sem analise. O corpo do report deve renderizar Markdown real, nao texto escapado com `###`/`**` visiveis. A tela nao deve bloquear a geracao apenas porque o Supabase esta indisponivel; nesse caso, gerar e mostrar a analise na sessao, avisando que nao foi salva no historico online.
- **Market Report inteligente:** Antes de chamar IA externa, montar um painel macro deterministico com S&P 500, Nasdaq, Dow, VIX, DXY, US10Y/US30Y, petroleo, ouro, USDBRL, IBOV/EWZ quando disponiveis. O prompt da IA deve receber esse painel processado, as implicacoes intermercados e os dados brutos apenas como apoio. O fallback local deve usar o mesmo painel para gerar uma leitura profissional de regime (`Risk-on`, `Risk-off`, `Neutro/seletivo`), calendario, riscos e vies tatico, sem citar WIN nem recomendacao de day trade.
- **Prompt do Market Report:** O prompt deve seguir hierarquia institucional: curva/yields -> DXY -> VIX -> indices EUA -> commodities -> Brasil/emergentes -> calendario/noticias. Incluir explicitamente o regime da curva americana, mas nao usar a ultima leitura do Analista IA Macro Global como contexto para evitar que o report apenas replique a leitura anterior. Exigir conclusoes no padrao dado observado -> leitura macro -> impacto provavel, evitar analise isolada de ativos, separar drivers, Global vs Brasil, calendario/cenarios, riscos e vies tatico.
- **Analista IA Macro Global:** A analise do Terminal de Trading/Market Report deve usar `execution/ai_analyst.py` em duas camadas: primeiro um motor local calcula score, regime macro, confianca, curva americana, drivers e calendario; depois Gemini apenas redige a leitura final quando houver chave. O prompt deve falar em regime macro (`Risk-on`, `Risk-off`, `Neutro/seletivo`), nao em call direta de compra/venda, conectando a cadeia juros -> dolar -> risco -> emergentes -> Brasil. O JSON `ai_insight` deve salvar `macro_regime`, `confidence`, `macro_score`, `curve_regime`, `curve_bias` e `drivers`, e o historico deve preservar esses campos para explicar mudancas de direcao.
- **Atualizacao manual do Analista IA:** O Analista IA Macro Global nao deve rodar automaticamente em cron dedicado. A pagina Market Report deve ter um botao `Atualizar Analista IA Macro`, que prepara o contexto local a partir de `mercados_globais`, `dados_mercado` e `calendario_economico`, gera `ai_insight`, salva no Supabase e atualiza `ai_insight_history` com as ultimas 5 analises.
- **Calendario no Market Report:** A analise do Market Report deve incluir o calendario economico do dia, priorizando eventos `HIGH` e `MEDIUM` em horario de Brasilia. Na geracao do report, buscar primeiro o calendario ao vivo do Investing.com para obter Atual/Projecao/Anterior; se o Investing falhar no Streamlit Cloud, usar `app_state.calendario_economico` do Supabase antes do arquivo local `calendario_economico.json`. Quando o Market Report baixar o calendario ao vivo, sincronizar tambem `calendario_economico` no Supabase. Para cada evento relevante, destacar atual/projecao/anterior e uma interpretacao breve dos cenarios possiveis: acima da projecao, abaixo da projecao, hawkish/dovish ou impacto em juros, DXY, petroleo e indices.
- **Feed de noticias ao vivo:** Para baixa latencia sem travar o Streamlit Cloud, buscar Financial Juice RSS direto no Streamlit/bridge com `fast_mode=True`, `translate=False`, cache de tela em torno de 30s e `min_network_interval=30`. No dashboard, exibir manchetes/resumos originais em ingles e nao preencher `title_pt`/`summary_pt` no caminho critico. O Supabase deve ser usado como cache/backup, nao como unica fonte quando ja existir dado antigo.
- **Fontes externas do feed no Streamlit Cloud:** Nao chamar GDELT/Reuters diretamente no carregamento do Streamlit, porque essas fontes podem responder `429` e prender o app no spinner. O caminho critico do feed deve usar Supabase/cache + Financial Juice RSS direto; fontes extras ficam para rotinas externas ou cache ja persistido.
- **Historico do feed:** O feed de noticias nunca deve ficar vazio enquanto busca novos dados. Se a fonte ao vivo ainda estiver carregando ou falhar, exibir as ultimas 10 noticias do cache local/Supabase/session_state e atualizar a lista assim que chegarem manchetes novas.
- **Indicadores Globais:** A aba Terminal Global deve manter uma cesta ampla de ativos acompanhados via `execution/fetch_global_markets.py`, incluindo indices globais, principais pares de FX, treasuries, emergentes/Brasil, ETFs setoriais, commodities e cripto. Antes de subir alteracoes nessa cesta, testar `fetch_global_data(save_file=False)` para confirmar que o Yahoo Finance retorna dados suficientes sem sobrescrever cache antigo.
- **Layout dos Indicadores Globais:** Renderizar os indicadores como heatmap compacto de cards, com categorias laterais, preco em destaque, variacao percentual por cor e campos H/L quando disponiveis. Evitar voltar para tabela simples nessa secao do Terminal Global.
- **Curva de juros americana no Terminal Global:** Abaixo dos Indicadores Globais, manter um painel local/deterministico de regime da curva dos Treasuries. Usar FRED quando `FRED_API_KEY` estiver disponivel (`DGS2`, `DGS5`, `DGS10`, `DGS30`, `DFII5`, `DFII10`, `T5YIE`, `T10YIE`) e cache de `mercados_globais` como fallback parcial. Classificar em `Bull/Bear Steepening`, `Bull/Bear Flattening`, `Parallel Up/Down` ou `Neutro`, sempre tratando a curva como filtro macro e nao como gatilho isolado de entrada.
- **Koyfin Embed no Terminal Global:** O embed compartilhado do Koyfin deve ficar ao lado do painel de curva de juros americana, como referencia visual complementar. Usar somente URL compartilhada limpa em iframe (`https://app.koyfin.com/share/371c5db6a3/simple`) e nao depender desse iframe para dados, IA, alertas ou calculos internos. Se o Koyfin bloquear iframe/login, o restante do dashboard deve continuar carregando normalmente.
- **Graficos proprios:** A pagina `Graficos Avancados` pode complementar os widgets TradingView com um componente proprio em `lightweight_chart_component.py`, embutido via `st.components.v1.html`, usando Lightweight Charts v5 (`chart.addSeries(...)`), dados Binance REST/WebSocket para cripto e proxies yfinance pre-carregados para mercado americano/macro. Os tempos padrao devem ser `30s`, `1m`, `5m`, `h1`, `1d`, `1w` e `1month`. Para yfinance, usar 1m como base intraday, agregar localmente 5m/h1 e usar serie diaria para 1d/1w/1month; avisar que 30s usa a base de 1m por falta de granularidade. Manter os widgets TradingView existentes como fallback ate o modulo proprio estar completamente validado.
- **Fontes gratuitas no Lightweight:** O componente proprio de graficos nao deve usar Finnhub como fonte de candles; reservar a API Finnhub para outras solucoes futuras. Nos graficos, usar Binance para cripto, yfinance para proxies de mercado e `FRED_API_KEY` para series macro FRED. As chaves devem ser lidas apenas no backend Streamlit (`st.secrets` ou variaveis de ambiente), nunca expostas no JS. Se FRED for selecionado em timeframe intraday, ajustar automaticamente para `1d`, porque FRED entrega series macro diarias.
- **Alpha Vantage:** Quando `ALPHA_VANTAGE_API_KEY` estiver configurada, usar Alpha Vantage como fonte complementar backend para cotações, forex, cripto, séries diárias/semanais/mensais e fallback de ativos onde yfinance falhar. Respeitar limites do plano gratuito com cache agressivo e nunca expor a chave no frontend/JS.
- **Twelve Data:** Quando `TWELVE_DATA_API_KEY` estiver configurada, usar Twelve Data como fallback intraday backend antes da Alpha Vantage para o Market Moving e outros candles multiativos. Ler a chave apenas de `st.secrets`/ambiente, usar cache e respeitar limites do plano gratuito. Para futuros/indices sem ticker direto, usar ETFs equivalentes (`USO`, `BNO`, `XLE`, `SPY`, `QQQ`, `UUP`, `GLD`, `EWZ`).
- **London Strategic Edge:** Quando `LSE_API_KEY` estiver configurada, usar `lse-data` (`from lse import LSE`) como fallback backend opcional para candles/cotacoes, sem expor a chave no frontend/JS. Aceitar tambem os aliases `LONDON_STRATEGIC_EDGE_API_KEY`, `LONDON_STRATEGIC_EDGE_KEY` e `LSE_KEY`. O roteador de cotacoes deve tratar London como candidato fresco entre Brapi/Twelve/Alpha e manter falha silenciosa se o pacote ou a API estiverem indisponiveis. Para quase-tempo-real, usar `execution/lse_realtime_collector.py` em job separado (`--loop --interval 5`) gravando `app_state.lse_realtime_quotes`; o Streamlit deve apenas aplicar esse overlay ao `mercados_globais` cacheado, sem abrir WebSocket dentro da pagina.
- **Diagnostico London free:** Como o plano London pode limitar endpoints, usar `execution/lse_diagnostics.py` para testar de forma leve `tier`, `symbols`, `catalog`, `economic_calendar`, `bond_yields` e candles de uma cesta curta. Salvar apenas status/contagens em `app_state.lse_diagnostics`, sem secrets. O painel `Saude das fontes` deve mostrar `London API` a partir desse cache, sem disparar chamadas externas no render.
- **News API:** Quando `NEWS_API_KEY` estiver configurada, usar News API como fonte complementar do feed de noticias, ticker superior, Market Moving e contexto do Market Report. Buscar apenas manchetes macro/mercado em ingles, normalizar para o mesmo schema do Financial Juice, deduplicar por link/titulo e cachear por 15 minutos para respeitar limites do plano gratuito. Nao expor a chave no frontend.
- **Painel operacional Lightweight:** O componente proprio deve manter crosshair com leitura OHLC/volume/variação/distancia da VWAP, linhas de abertura/maxima/minima/fechamento anterior/preco atual, VWAP diaria/semanal/mensal, bandas percentuais e por desvio, medias SMA/EMA configuraveis, volume relativo, Volume Profile da sessao com POC, VAH e VAL destacados, HV252 calculada com retornos logaritmicos de candles diarios e bandas intradiarias 1/1.5/2 sigma projetadas a partir do fechamento anterior, markers de sinais operacionais, alertas laterais, acoes de zoom/export/reload e preferencias em `localStorage`. Nao renderizar oscilador inferior para preservar area util do candle e reduzir custo de renderizacao.
- **REFS no Lightweight:** A camada `Refs` deve marcar maxima/minima do dia atual, dia anterior, semana anterior e mes anterior, com labels curtos no eixo (`DIA MAX`, `DIA ANT MAX`, `SEM ANT MAX`, `MES ANT MAX` etc.), cores diferentes por grupo e estilos de linha distintos para leitura rapida. Nao plotar abertura do dia nem fechamento anterior nessa camada.
- **Motor de sinais Lightweight:** Os markers do grafico proprio devem ser gerados por motor deterministico local, sem LLM e sem execucao de ordens. O motor ativo atual opera apenas sinais de reversao por regioes de volatilidade (`VOL_BUY`/`VOL_SELL`), aplica score minimo, cooldown, filtros configuraveis e retorna motivo curto com regiao, fonte, sigma e padrao de candle. A camada `Sinais` controla a exibicao dos markers, e o crosshair deve mostrar a explicacao do sinal quando houver marker no candle.
- **Filtros REV Lightweight obsoletos:** A regra antiga `REV_BUY`/`REV_SELL` por confluencia direta de VWAP/HV/GARCH foi substituida pelo motor de regioes de volatilidade. Nao usar RSI/IFR nem a logica antiga como criterio operacional.
- **Volume Profile e sinais:** `POC`, `VAH` e `VAL` devem permanecer apenas como leitura visual do Volume Profile. O motor de sinais REV/TREND nao deve usar `POC`, `VAH` ou `VAL` para regime, proximidade, stop ou alvo; usar referencias de dia, VWAP, swings recentes e bandas HV252 no lugar.
- **Motor de sinais por regioes de volatilidade:** O motor ativo deve usar exclusivamente regioes criadas a partir das `Bandas Vol` GARCH/HV252. Compra so pode ocorrer em regiao inferior com multiplicador minimo configuravel, candle tocando/varrendo a regiao, padrao de rejeicao comprador, volume ok quando habilitado, cooldown e score minimo. Venda so pode ocorrer em regiao superior com as mesmas confirmacoes espelhadas. Padroes validos: pin bar, engolfo, falso rompimento, fechamento de volta para dentro e impulso reversivo. A logica antiga de REV por VWAP/HV/GARCH direto fica obsoleta e nao deve ser usada como motor operacional.
- **Estrategia unica temporaria:** Enquanto a estrategia estiver em simplificacao, o grafico proprio deve operar somente o motor de reversao (`REV_BUY` e `REV_SELL`). `TREND_BUY` e `TREND_SELL` devem permanecer desativados, escondidos do painel e fora da lista de candidatos, mesmo que existam preferencias antigas no `localStorage`.
- **Grafico unico Lightweight:** A pagina `Graficos Avancados` deve renderizar apenas um componente Lightweight operacional enquanto houver somente uma estrategia ativa. Quando a estrategia TREND voltar, reavaliar se faz sentido separar novamente em dois graficos.
- **Altura do grafico Lightweight:** O grafico proprio do motor de reversao deve priorizar area util do candle, com altura interna em torno de `1320px` no desktop e iframe Streamlit alto o suficiente para nao cortar toolbar, painel lateral ou status.
- **Painel lateral Lightweight:** O painel lateral do motor de reversao deve ficar limpo e focado em risco operacional. Remover cards soltos como `Ultimo`, `VWAP/distancia`, `Volume`, `Weis Wave`, `Volume Profile`, `GARCH` antigo e `HV252`; manter `ATR 14` para referencia de stop, `GARCH intraday` calculado pelo timeframe atual, leitura de `Sinal/Candle`, configuracoes e alertas.
- **Ativos Lightweight Brasil/FX:** O grafico proprio em `Graficos Avancados` deve incluir ativos Brasil via `yfinance` (`.SA`, ETFs e `^BVSP`), `EURUSD` via `EURUSD=X` e referencias BCB gratuitas via SGS apenas para cambio BRL. Ativos BCB sao diarios e devem forcar timeframes `1d`, `1w` ou `1month`, sem WebSocket. Nao exibir Selic, CDI ou IPCA no seletor do Lightweight.
- **Ativos cripto Lightweight:** O grafico proprio deve incluir pares cripto via Binance para o motor de reversao: `BTCUSDT`, `ETHUSDT`, `SOLUSDT`, `LINKUSDT`, `BNBUSDT`, `DYDXUSDT`, `ENAUSDT`, `LDOUSDT`, `ARKMUSDT`, `PENDLEUSDT`, `AAVEUSDT`, `XAUTUSDT`, `PAXGUSDT` e `ONDOUSDT`. `MNTUSDT` e `HYPEUSDT` nao estao disponiveis no endpoint spot da Binance no momento da validacao; indices de dominancia como `BTC.D` e `USDT.D` tambem nao devem ser adicionados nessa lista enquanto a fonte for Binance, porque nao sao pares negociados no endpoint de candles.
- **Ativos FRED Lightweight:** No seletor do grafico proprio, manter FRED apenas para `US10Y FRED` e `US30Y FRED`. Nao exibir `Fed Funds`, `CPI FRED` ou `Unemp FRED`.
- **Desvios da VWAP:** O toggle `Desvios` do grafico proprio deve usar multiplicadores de desvio padrao `1`, `2` e `3`, mantendo as bandas percentuais da VWAP em toggle separado.
- **Bandas percentuais VWAP:** O grafico proprio deve ter toggles independentes para bandas percentuais da VWAP diaria (`Bandas D`) e da VWAP semanal (`Bandas W`), usando 0.5%, 1%, 1.5% e 2% com paletas bem diferentes por horizonte. Nao desenhar bandas percentuais da VWAP mensal.
- **Bandas semanais por volatilidade:** O grafico proprio deve ter um toggle separado `Bandas W Vol` para bandas adaptativas da VWAP semanal. Calcular o desvio padrao da distancia `close - VWAP semanal` dentro da semana e plotar VWAP semanal +/- 1, 2 e 3 desvios. Essa camada deve ser independente das bandas percentuais semanais.
- **Coloracao de candles por volume:** Os candles do grafico proprio devem destacar participacao por RVOL sem alterar o motor de sinais: RVOL normal usa verde/vermelho discretos, RVOL >= 1.2 reforca a cor direcional, RVOL >= 1.5 usa cores fortes, e RVOL >= 2 usa destaque ambar para volume extremo.
- **Weis Wave Volume:** O grafico proprio deve ter uma camada visual `Weis Vol` que acumula volume por pernadas de alta/baixa usando uma reversao percentual minima. Plotar histograma inferior com volume acumulado da onda atual, verde para alta e vermelho para baixa, e mostrar no painel lateral a direcao, volume da onda atual e comparacao com a ultima onda da mesma direcao. Essa camada deve ser visual primeiro e nao alterar o motor REV sem validacao posterior.
- **TTS GARCH(1,1) + HV252 - Modelo Hull:** O grafico proprio deve ter uma camada `GARCH` baseada em GARCH(1,1) sem dependencias externas, com `omega=0.000001`, `alpha=0.08`, `beta=0.90` e validacao `alpha + beta < 1`. Por padrao, calcular o GARCH em base diaria usando `dailyCandles` e apenas projetar as bandas no grafico intraday; modo intraday pode existir apenas para comparacao. Plotar zonas horizontais lognormais/Hull em torno da referencia por 0, 0.5, 1, 1.5, 2, 2.5 e 3 sigma usando `referencia * exp(sigma * multiplicador)`, mostrar no painel volatilidade atual, timeframe do GARCH, preco de referencia, modo de referencia e distancia em sigma por log-retorno. A referencia deve poder usar fechamento anterior, abertura da sessao, VWAP, ultimo fechamento ou ajuste manual. Quando `Filtro GARCH` estiver ligado, o motor REV deve permitir compra apenas abaixo de `-minSigmaForSignal` e venda apenas acima de `+minSigmaForSignal`, com padrao `1.5`.
- **Bandas de Volatilidade:** O grafico proprio deve ter um indicador visual chamado `Bandas Vol`, separado do motor de sinais, que plota em uma unica camada a referencia central, bandas GARCH lognormais 0.5/1/1.5/2/3 sigma e bandas HV252 1/2/3 sigma. Essa camada substitui a plotagem visual separada de `GARCH` e `HV 252` para evitar duplicacao de linhas; o checkbox `Filtro GARCH` no painel de sinais continua controlando apenas a validacao do motor REV.
- **Traducao manual do feed:** O feed deve carregar em ingles por padrao. Se o usuario clicar em `Traduzir noticias`, traduzir apenas os cards visiveis no Streamlit, mantendo a busca de rede em `translate=False` para nao atrasar o carregamento inicial.
- **Traducao completa sob demanda:** Ao clicar em `Traduzir noticias`, traduzir todo o feed filtrado para portugues do Brasil usando `translate_text_google` e cache em `st.session_state`, nao apenas substituicoes heuristicas. O botao deve continuar sem afetar o carregamento inicial em ingles.
- **yfinance rate limit:** Downloads em batch de 20+ tickers causam `YFRateLimitError`. Solução: mini-lotes de 5 tickers, delay 3s entre lotes, `threads=False`, retry individual com backoff 5s.
- **Windows cp1252:** Console do Windows não suporta emojis Unicode (▲▼🔴🟡🟢). Usar caracteres ASCII no console, emojis só no Streamlit (UTF-8).
- **DI Futuro B3:** Contratos DI1F26/DI1F27 não estão disponíveis no yfinance. Precisa de scraping direto da B3 ou API especializada.
- **Investing.com:** Scraping do calendário econômico pode falhar (403/CloudFlare). Fallback estático por dia da semana implementado.
- **Cotações globais no Streamlit Cloud:** O painel deve buscar `mercados_globais` direto via `execution/fetch_global_markets.py` com cache curto no app e usar Supabase apenas como fallback. Nao depender exclusivamente do `dashboard_bridge.py`, porque ele roda localmente e, se estiver offline, deixa os dados congelados no horario do ultimo sync.
- **Calendario economico com Atual:** O endpoint Faireconomy/ForexFactory (`ff_calendar_thisweek.json`) normalmente nao entrega o campo `actual`; tentar Investing.com primeiro com cache curto e timeout controlado para `Atual/Projecao/Anterior`, mantendo Supabase/Faireconomy como fallback quando houver bloqueio/rate limit. Nunca deixar falha do Investing derrubar a renderizacao.
- **IA Macro TTS do calendario:** A interpretacao do calendario deve ser deterministica e local durante a renderizacao. Nao chamar LLM/API externa no Streamlit para esse card. Usar `execution/macro_calendar_ai.py` para normalizar evento, detectar `actual`, calcular surpresa, classificar choque macro e gerar Score Risk/viés operacional.
- **Investing Brasil para calendario ao vivo:** Para o Terminal Global, usar `br.investing.com/economic-calendar/Service/getCalendarFilteredData` com `Session`, GET inicial em `/economic-calendar/`, cookies e `timeZone=8` antes do fallback `www.investing.com`. Essa rota entrega os campos `Atual/Projecao/Anterior` iguais ao widget visual, enquanto o iframe oficial continua servindo apenas como conferencia visual e nao deve ser tratado como fonte legivel pelo backend.
- **Numeros BR no calendario:** Valores do Investing Brasil podem vir com virgula decimal (`55,1`, `0,4%`). O parser da IA deve converter virgula decimal corretamente antes de calcular surpresa; quando nao houver consenso, usar o valor anterior como benchmark secundario e marcar a surpresa como `vs anterior`.
- **Score Macro TTS:** O score Risk-on/Risk-off deve combinar surpresa do dado, categoria do indicador, regime dominante e confirmacao intermercado. Inflação/salarios acima pesam risk-off; inflação abaixo pesa risk-on. Atividade/emprego fortes pesam risk-on em regime neutro/recessivo, mas podem pesar hawkish/risk-off em regime de inflação dominante. A confirmação deve olhar US10Y, DXY, VIX, S&P, Nasdaq, EWZ, USD/BRL, Bitcoin e energia, mantendo o score final entre -100 e +100.
- **Modo Investing-only no Terminal Global:** Quando o usuário pedir análises exclusivamente com dados do Investing, chamar `interpret_event(event, None)` e filtrar histórico para `source == "Investing.com"`. Nesse modo, nao usar `get_global_markets_data()`, Supabase/Faireconomy ou qualquer confirmação intermercado na IA; o texto deve deixar claro que o score vem apenas da surpresa do calendário Investing.
- **Leitura de projeção:** Se o evento Investing ainda nao tiver `Atual`, mas tiver `Projecao` e `Anterior`, a IA deve gerar status `Projecao analisada`, comparando `Projecao vs Anterior`. Essa leitura é pre-evento, deve ter peso menor que um dado divulgado e deixar claro que a entrada operacional depende da confirmação do campo `Atual`.
- **Visual da IA sem placar:** Na tela do Terminal Global, nao exibir numero/placar do score da IA. Mostrar de forma textual se houve surpresa, qual foi o choque macro e qual efeito esperado (`Risk-on`, `Risk-off` ou `Neutro`). O score pode existir internamente apenas para classificar o efeito.
- **Foco macro da IA do calendario:** A leitura da IA do calendario nao deve citar WIN nem gerar vies operacional para mini indice. Focar o impacto em juros, inflacao, DXY, petroleo e indices americanos (S&P 500, Nasdaq e Dow Jones).
- **IA Macro TTS curta e intermercado:** No Terminal Global, a IA Macro TTS deve responder de forma curta e direta. A leitura deve combinar calendario Investing com `get_global_markets_data()` para avaliar correlacao intermercados, regime dominante, politica monetaria, inflacao, juros, DXY, petroleo e indices americanos. O texto deve evitar explicacoes longas e apresentar apenas surpresa/projecao, vies Risk-on/Risk-off, choque macro e principais efeitos.
- **Foco USD/BRL da IA Macro TTS:** Na escolha dos proximos eventos do Terminal Global, priorizar eventos dos EUA/USD e Brasil/BRL antes dos demais paises/moedas. O card de proximos eventos e a analise IA devem focar primeiro em Fed, inflacao, emprego, PMI/ISM, treasuries, BCB/Copom, Selic, IPCA, fiscal e real; se nao houver evento USD/BRL disponivel, usar o calendario global como fallback.
- **Historico IA Macro TTS:** No Terminal Global, o historico da IA Macro TTS deve registrar/renderizar apenas eventos economicos com moeda `USD` ou `BRL`, mantendo no maximo os ultimos 5 eventos divulgados desse filtro.
- **Layout calendario Terminal Global:** Manter a area de analise IA Macro TTS lado a lado com o widget oficial do calendario Investing em desktop. A coluna esquerda deve concentrar proximos eventos, IA e historico; a direita deve exibir o iframe do Investing com altura suficiente para leitura.
- **Painel lateral de correlacao Terminal Global:** O Terminal Global deve dividir o corpo em conteudo principal e uma lateral direita compacta, renderizada apenas em `pagina_terminal_global()`. A lateral deve conter o painel de correlacao macro em TradingView com `USA500`, `TVC:GOLD`, `TVC:UKOIL`, `OTCB:US10Y`, `OTCB:US30Y` e `CAPITALCOM:DXY`, com GOLD logo abaixo do S&P 500, seletor de intervalo e comportamento sticky no desktop. Em telas menores, ocultar essa lateral para nao sobrepor graficos ou widgets.
- **Graficos Terminal Global:** A aba Terminal Global deve exibir quatro graficos TradingView em duas linhas, dois lado a lado em cada linha, todos com seletores independentes de ativo/intervalo, volume oculto, painel lateral de desenho habilitado e VWAP diaria/semanal/mensal.
- **Ativos dos graficos globais:** Os seletores do Terminal Global devem incluir `USATEC` (`ACTIVTRADES:USATEC`), `ETHUSDT` (`BINANCE:ETHUSDT`), `US10Y OTCB` (`OTCB:US10Y`) e `US30Y OTCB` (`OTCB:US30Y`).
- **Defaults dos graficos globais:** Os quatro graficos do Terminal Global devem abrir em `5 minutos`: grafico 1 `USA500`, grafico 2 `UKOIL`, grafico 3 `BRA50` e grafico 4 `BTCUSDT`.
- **Classificacao visual de noticias:** O Terminal Bloomberg deve classificar manchetes por impacto (`URGENTE`, `ALTO IMPACTO`, `IMPACTO MEDIO`, `BAIXO IMPACTO`), ordenar o feed priorizando maior impacto antes da recencia e destacar visualmente as noticias urgentes/alto impacto com cor, borda e badge.
- **NEWS compacta na sidebar:** A barra lateral deve ter uma aba `NEWS` ao lado de `MERCADOS` e `CALENDARIO`, usando o mesmo feed do Terminal Bloomberg via `load_bloomberg_news_feed`. Mostrar no maximo 10 manchetes compactas, em ingles por padrao, com horario, fonte, link e badge de impacto; nao criar novas rotas de rede alem do cache/Supabase/Financial Juice ja usados pelo feed principal. A secao deve ter botoes compactos `Atualizar`, `Traduzir`/`Ver EN`, `-` e `+`; atualizar limpa o cache do feed, traduzir converte somente as manchetes visiveis da sidebar para PT-BR, e `-`/`+` controlam apenas o zoom/fonte do feed NEWS.
- **Logo na sidebar:** A barra lateral deve exibir a logo `assets/trading_strategy_logo.png` acima da navegacao principal. Nao referenciar caminhos locais fora do repo, como `Downloads`, porque o Streamlit Cloud nao consegue acessar esses arquivos.
- **Relogio na sidebar:** A barra lateral deve exibir um relogio compacto logo abaixo da navegacao principal, com horario de Brasilia atualizado por segundo via componente HTML/JS, fonte grande e marcadores de abertura `09:00 Abertura BR`, `10:00 Abertura acoes BR` e `10:30 Abertura NY`, destacando proximo evento, evento em andamento e eventos ja passados.
- **Pagina inicial:** No primeiro acesso, o dashboard deve abrir no `Terminal Global` como pagina padrao da navegacao lateral.
- **Graficos globais do Terminal Global:** Os quatro widgets `Grafico Global` devem abrir em intervalo fixo de 5 minutos e estilo area (`style: "3"`), nao candle, para leitura rapida de fluxo intraday.
- **Global Momentum Screener:** O Terminal Global deve ter um monitor leve `TTS Global Momentum Screener`, baseado em `execution/global_momentum_screener.py`. O modulo deve usar universo global curado, yfinance/cache local, fatores diarios de momentum 21/63/126, volatilidade 21/63, momentum ajustado por volatilidade, forca relativa, aceleracao, estrutura de tendencia e RVOL quando disponivel. Tambem deve ter modo `Intraday` com cache proprio `.tmp/global_intraday_momentum_screener.json`, candles yfinance `5m`, fatores de fluxo 5/15/30/60 minutos, momentum desde abertura, aceleracao, forca relativa intraday, posicao no range do dia e RVOL quando disponivel. A UI deve ler cache primeiro e recalcular somente por botao `Atualizar Diario`/`Atualizar Intraday` ou cache expirado, evitando chamadas pesadas em todo rerender. Exibir regime global, classe lider/fraca, top comprador/vendedor e resumo por classe; nao adicionar graficos pesados nesta fase.
- **MONITOR MACRO FRED:** A pagina `MONITOR MACRO` deve usar `execution/macro_fred_monitor.py` como motor local/deterministico para interpretar o cenario macro dos EUA com foco em FRED e eventos economicos USD/BRL da semana. A primeira fase nao cria FastAPI, banco novo nem ALFRED completo; ela usa FRED estrutural, calendario economico ja disponivel no dashboard, cache local `.tmp/macro_fred_monitor.json` e botao manual `Atualizar Monitor Macro`. Para evitar travamento no Streamlit, a pagina deve carregar apenas cache no render normal, inclusive cache stale com aviso; chamadas ao FRED/calendario externo so podem ocorrer ao clicar no botao manual. O motor deve separar blocos de inflacao, trabalho, atividade, condicoes financeiras e recessao, calcular valor atual/anterior, delta, z-score/percentil quando possivel, score interpretavel e drivers. Eventos economicos devem ser classificados por atual/projecao/anterior, impacto oficial em touros e direcao macro (`inflacao_mais_quente`, `inflacao_mais_fria`, `trabalho_apertado`, `atividade_fraca` etc.), compilando a leitura semanal em regime macro, vies Fed e leitura de risco. A UI deve separar explicitamente `FRED` estrutural de `Semana`/surpresa marginal, mostrar um resumo semanal quantitativo curto, principais drivers FRED e indicar quando a surpresa reforca ou contraria o quadro estrutural.
- **Historico MONITOR MACRO:** Cada atualizacao real do `MONITOR MACRO` deve salvar snapshot leve em `.tmp/macro_fred_history.json`, preservando regime, vies Fed, leitura de risco, score macro e scores de inflacao/atividade/trabalho/condicoes/recessao. A pagina deve renderizar as ultimas leituras em cards compactos para acompanhar mudancas de regime sem depender de banco novo.
- **Impacto do calendario Investing:** O impacto do evento economico deve seguir a escala oficial de touros do Investing (`bull1`, `bull2`, `bull3`). Um touro = baixo impacto, dois touros = impacto medio, tres touros = alto impacto. A IA deve ponderar a surpresa por essa escala: quanto mais touros, maior o efeito macro.
- **Horario do calendario Investing:** Usar `timeZone=12` nas chamadas e no widget do Investing para exibir os eventos no horario de Brasilia/Sao Paulo. `timeZone=8` deixava o widget em GMT-4 e atrasava a agenda em uma hora.
- **Performance Streamlit:** Fragmentos RTD que leem a mesma chave do Supabase devem compartilhar cache curto (`ttl=2`) em vez de consultar o banco a cada rerender. Fallbacks de `mercados_globais`, `calendario_economico` e `financial_juice_news` devem usar cache de 30s para evitar latencia e rate limit quando a fonte ao vivo falhar.
- **Boot do Streamlit Cloud:** No carregamento inicial, `get_global_markets_data()` e `get_calendar_data()` devem ler Supabase/cache primeiro. Nao chamar yfinance nem endpoint backend do Investing antes de renderizar a UI, pois qualquer timeout externo pode deixar o app preso no spinner.
- **Saude das fontes:** Cada fonte critica deve registrar status em `.tmp/source_health.json` via `execution/source_health.py`, usando `ok`, `stale`, `error` ou `disabled`. O Terminal Global deve mostrar um painel compacto de saude das fontes sem disparar novas chamadas externas. Quando uma fonte falhar, preferir mostrar cache stale com aviso em vez de esconder dado ou travar a pagina.
- **WATCHLIST IA TTS:** A pagina `WATCHLIST` deve separar Brasil Acoes, EUA ETFs Setoriais, Cripto, Moedas/Forex, Commodities e Metais, sem misturar os rankings. A Watchlist deve manter apenas recomendacoes de `Position`/Position Trade; nao renderizar nem gerar radar Swing. Usar `execution/watchlist_ai.py` com motor local/deterministico via yfinance + `mercados_globais`, gerando position trades LONG (`direcao=compra`) e SHORT (`direcao=venda`) quando o modelo confirmar. A decisao deve separar score direcional de score operacional: aplicar filtro duro de tendencia/forca relativa, evitar compra/venda atrasada quando o ativo estiver esticado demais, usar regime por classe para favorecer LONG em risk-on e SHORT em risk-off, e exibir os filtros aprovados/bloqueios no card. Entrada deve buscar pullback tecnico em MA20/MA50/suporte ou resistencia recente; stop deve combinar ATR com suporte/resistencia recente. Para venda, inverter niveis: gains abaixo da entrada, loss acima da entrada e resultado positivo quando o preco cair apos ativacao. Cada recomendacao deve incluir ticker yfinance e mini grafico Lightweight Charts em 4h, agregando candles de 1h do yfinance, com linhas horizontais de preco atual, entrada, gain parcial (`gain_1`), gain final e loss; buscar candles com cache curto para nao recarregar yfinance em todo rerender. O grafico deve marcar com circulo o candle mais recente cujo range tocou a entrada, indicando ativacao do trade, e o card deve exibir data/hora de ativacao em horario de Brasilia ou `Aguardando entrada` quando a entrada ainda nao foi tocada. O campo `Resultado` do card so deve contabilizar percentual apos a entrada ser ativada; antes disso mostrar `Aguardando`. Registros de take/stop em `.tmp/watchlist_results.json` tambem devem exigir `entrada_ativada=True`, evitando contabilizar ganhos/perdas de trades que ainda nao acionaram. Baixar yfinance em mini-lotes para reduzir rate limit. Quando o historico yfinance falhar ou vier insuficiente, usar `mercados_globais` do proprio dashboard como radar rapido para preencher preco, variacao, range do dia e score simplificado, marcando `fonte_sinal='dashboard'` em vez de deixar blocos vazios. O fallback deve casar por ticker, simbolo e aliases dos nomes amigaveis do dashboard (`BITCOIN`, `ETHEREUM`, `SOLANA`, `BRENT OIL`, `WTI OIL`, `NATURAL GAS`, `GOLD`, `SILVER`, `COPPER`, `USDBRL (Comercial)` etc.), pois os nomes do painel global nem sempre sao iguais aos simbolos internos. Salvar o ultimo payload valido em `.tmp/watchlist_payload.json` e reutilizar se todas as fontes falharem. Recomendacoes com acao `comprar` ou `vender` que baterem `gain_1`, `gain_2`, `gain_final` ou `loss` devem ser registradas em `.tmp/watchlist_results.json`, sem duplicar o mesmo ativo/tipo/entrada/nivel, e renderizadas abaixo da Mesa WATCHLIST com data, direcao, saida e resultado percentual.
- **WATCHLIST QUANT:** A pagina `WATCHLIST QUANT` deve usar `execution/watchlist_quant.py` como motor local/deterministico, reaproveitando o universo da Watchlist e dados yfinance/cache. A pagina deve entregar screenings de Momentum/Trend Following, Mean Reversion por z-score, Pairs/StatArb por correlacao + z-score do spread/ratio, Event-driven por impulso recente/z-score, Volatility por compressao/expansao de volatilidade e Crypto Quant apenas para ativos cripto. Cada ranking deve mostrar score, direcao/regime, entrada, stop/alvo ou par comprado/vendido, usando o proprio dashboard como universo de ativos; nao usar IA generativa nem execucao automatica.
- **Volatility Regime Engine:** Inspirado em research quantitativo institucional, manter a implementacao leve e deterministica: comparar volatilidade curta (`vol20`) com volatilidade estrutural (`vol252`), z-score de 20 periodos, sigma de 252 periodos e ATR como unidade de risco. A Watchlist Quant deve classificar compressao, expansao inferior/superior e reversao por volatilidade, enquanto o grafico proprietario deve mostrar no painel lateral `Regime Vol` com HV252, GARCH e ATR sem adicionar chamadas externas no render. O painel lateral tambem deve ter `Risco / Stop`, calculando stop base por ATR, alvo estatistico mais proximo (VWAP, fechamento anterior ou maxima/minima do dia) e distancia ate a banda de volatilidade mais proxima.
- **Market Moving:** A pagina `Market Moving` deve reaproveitar o feed de noticias ja existente (`load_bloomberg_news_feed`) e filtrar apenas headlines de alto impacto/urgentes. Tambem deve incluir eventos economicos ja divulgados do calendario com foco em USD/BRL, marcando esses cards como `DATA`. Usar `execution/market_moving.py` para mapear noticia/evento -> ativos proxy (energia, DXY, indices, ouro, cripto, Brasil), buscar candles intraday via yfinance e calcular reacao pos-evento em 5/15/30 minutos. A renderizacao deve usar cards vermelhos com tags e graficos Lightweight Charts marcando o candle/horario da noticia/evento com um circulo grande sobre o candle de 5 minutos que contem o timestamp exato. Timestamp de noticia deve ser tratado como epoch UTC, exibido em America/Sao_Paulo e convertido para o timezone do indice yfinance antes de recortar candles ou posicionar marcador; nunca usar candles de fallback fora da janela do evento, pois isso marca a noticia no horario errado. Quando yfinance nao entregar candle na janela curta, procurar ate 36h depois e marcar como `ABERTURA` o primeiro candle negociado apos a noticia/evento. Quando yfinance nao entregar dados, tentar Twelve Data e depois Alpha Vantage como fallback por ativo com proxies equivalentes (`USO` para WTI, `BNO` para Brent, `XLE` para energia, `SPY/QQQ/UUP/GLD/EWZ` para macro), mostrando a fonte no titulo. Nao usar BTC/ETH como fallback generico para noticias de energia/geopolitica; esses ativos so devem aparecer quando a propria noticia for classificada como cripto. Eventos sem nenhum candle nos ativos mapeados devem ficar como `pending_open` e nao renderizar cards vermelhos vazios; mostrar apenas aviso compacto de que aguardam abertura/candle.
- **Hub Macro de Noticias:** A pagina `Market Report` deve exibir um hub compacto de manchetes macro usando `execution/news_macro_hub.py`. O coletor deve priorizar fontes abertas/RSS/API publica das camadas N1/N2 (Bloomberg RSS, CNBC, Fed, ECB, BCB, U.S. Treasury, IMF, World Bank, Investing, CME, Nasdaq, CoinDesk e GDELT para Reuters/FT/WSJ/Trading Economics/B3/NYSE/The Block), sem burlar paywall ou login. O hub deve classificar impacto (`ALTO`, `MEDIO`, `BAIXO`), temas macro, ativos afetados e vies (`Risk-on`, `Risk-off`, `Neutro`), usar cache local `.tmp/news_macro_hub.json` e cache Streamlit curto para nao travar a pagina. Se as fontes ao vivo falharem, reutilizar o ultimo payload valido.
- **Supabase Auth Admin:** `supabase.auth.admin.list_users()` pode retornar pagina com atributo `.users` ou uma lista direta, dependendo da versao do client. Normalizar esse retorno antes de procurar email; se nao fizer isso, o fallback de confirmar `Email not confirmed` pode falhar silenciosamente.
- **Seguranca Supabase/app_state:** Toda leitura/escrita de `app_state` no Streamlit deve passar por chaves conhecidas em allowlist e toda escrita deve usar `sync_app_state_value()`, nunca `supabase.table("app_state").upsert(...)` direto na UI. Nao gravar credenciais, senhas, tokens ou secrets em `app_state`. Manter RLS com escrita apenas para admin via funcao `public.is_profile_admin()` `security definer`, com allowlist de chaves operacionais.
- **Seguranca HTML/links:** Qualquer texto vindo de feeds, calendario, Supabase/cache ou arquivos JSON e renderizado com `unsafe_allow_html=True` deve passar por `sanitize_text()`/`html.escape`. Links externos em cards devem passar por `safe_external_url()` e aceitar apenas `http://` ou `https://`, usando `#` como fallback para evitar `javascript:`/URLs maliciosas.
- **Jobs GitHub/Supabase:** Rotinas server-side que escrevem em `app_state` (`run_cloud_updates.py`, `run_market_report_update.py` e workflows) devem usar uma chave server/service do Supabase. Neste projeto, o padrao atual no Streamlit/GitHub Secrets e `SUPABASE` para a URL e `SUPABASE_SERVICE` para a chave server/service. Tambem aceitar aliases legados `SUPABASE_URL`, `SUPABASE_KEY` e `SUPABASE_SERVICE_ROLE`. Nunca usar chave anon/public para escrita. Centralizar escrita em `execution/app_state_sync.py`, com allowlist de chaves e erro claro quando a chave server estiver ausente.
- **GitHub Actions London Realtime:** O workflow `.github/workflows/lse_realtime_quotes.yml` deve rodar 24/7 a cada 5 minutos com `--ignore-market-window`, pois alguns ativos/cripto/FX podem atualizar fora da janela B3. GitHub Secrets devem conter `LSE_API_KEY`, `SUPABASE` e `SUPABASE_SERVICE` (ou aliases aceitos). O job executa uma passada curta (`python execution/lse_realtime_collector.py --once --ignore-market-window`) para atualizar `app_state.lse_realtime_quotes`; nao usar loop infinito dentro do GitHub Actions. O overlay no Streamlit deve aceitar alguns minutos de tolerancia entre rodadas do GitHub Actions.
- **Cache local Lightweight yfinance:** O componente Lightweight deve salvar o ultimo payload valido do yfinance em `.tmp/lightweight_yahoo_payload.json` e reutilizar esse cache por curto periodo quando o Yahoo retornar vazio/timeout. Isso reduz tela `Sem dados` e evita que uma oscilacao externa trave a pagina de Graficos Avancados.
- **Layout Lightweight responsivo:** Na pagina `Graficos Avancados`, o grafico proprietario deve evitar alturas fixas grandes. Usar layout baseado em viewport dentro do componente, `ResizeObserver` para aplicar largura e altura reais no Lightweight Charts, lateral ao lado no desktop e lateral abaixo no mobile. O iframe Streamlit deve permitir rolagem em mobile para nao cortar painel/configuracoes.
- **Labels Lightweight compactos:** As linhas de preco do grafico proprietario devem mostrar labels apenas para referencias essenciais (`ATUAL`, maxima/minima do dia, referencias anteriores, `VAH`/`VAL`). Regioes de bandas (`reg sup`/`reg inf`) e bandas intermediarias `BV GARCH`/`BV HV` devem continuar plotadas, mas sem label no eixo para nao poluir a leitura do candle.
- **Top Movers Brasil no Terminal de Trading:** A pagina `Terminal de Trading` deve exibir um ranking compacto de maiores altas e maiores baixas de acoes brasileiras liquidas. Usar Brapi como primeira fonte gratuita para B3, em lotes de ate 20 ativos por request e chave opcional via `BRAPI_TOKEN`/`BRAPI_API_KEY` no backend. Manter yfinance `.SA` como fallback com cache curto, comparando o ultimo preco intraday com o fechamento do pregao anterior. O ranking nao deve bloquear o carregamento do terminal; se a fonte estiver indisponivel, mostrar aviso compacto e tentar novamente no proximo ciclo.
- **Grafico comparativo no Terminal de Trading:** A pagina `Terminal de Trading` deve exibir graficos de linha normalizados em percentual, lado a lado: S&P 500 (`^GSPC`) x Brent (`BZ=F`), EWZ (`EWZ`) x USDBRL (`BRL=X`) e 6L1 (`6L=F`) x DXY (`DX-Y.NYB`). Usar yfinance com cache curto e periodo intraday do dia atual em 5 minutos para comparar forca relativa entre risco global, petroleo, Brasil, cambio e dolar global. Renderizar o eixo X como sequencia continua de candles com labels de data/hora para evitar gaps visuais de fim de semana/fora de mercado.
- **Comparativo BRINTR x USINTR:** A pagina `Terminal de Trading` deve exibir um grafico Plotly proprio de linha com Selic/meta Brasil (`BCB SGS 432`) e Fed Funds EUA (`FRED DFF`), em escala regular de taxa, sem normalizacao percentual, abaixo dos comparativos intraday. Nao usar TradingView para esse comparativo: os embeds `Advanced Chart` e `Symbol Overview` podem bloquear ou nao renderizar series `ECONOMICS` fora do site TradingView. Para a perna Brasil, usar fallback em camadas quando o BCB falhar: Trading Economics API oficial quando houver `TRADING_ECONOMICS_API_KEY`/`TRADINGECONOMICS_API_KEY`/`TE_API_KEY`, Trading Economics Brazil Interest Rate via HTML, Boletim Focus/Selic em `app_state`, depois `regime_juros.taxa_sintetica`. A credencial publica antiga `guest:guest` da Trading Economics foi descontinuada e retorna HTTP 410.
- **Grafico EWZ leve no Terminal de Trading:** Abaixo dos comparativos, renderizar um grafico Plotly exclusivo do `EWZ` em linha no intervalo `5m`, usando apenas o dia anterior e o dia atual, incluindo pre-market/after-market quando disponivel. A fonte preferencial do grafico deve ser o proprio dashboard: primeiro usar a serie OHLCV salva em `app_state.ewz_plotly_ohlcv`/`.tmp/ewz_plotly_ohlcv.json`; quando houver coleta nova, atualizar esse cache interno. Se nao houver cache, usar o snapshot `mercados_globais` para renderizar uma linha minima do EWZ em vez de deixar a tela vazia. Como alimentacao do cache, tentar Yahoo Chart direto `query1.finance.yahoo.com/v8/finance/chart/EWZ` com `includePrePost=true`, depois Yahoo Chart regular, depois `yfinance` com/sem prepost, depois Twelve Data (`TWELVE_DATA_API_KEY`) e Alpha Vantage (`ALPHA_VANTAGE_API_KEY`) em 5 minutos. Plotar preco, VWAP diaria, VWAP semanal, VWAP mensal, fechamento diario anterior e Bandas Vol HV252 +/-1 e +/-2. As Bandas Vol desse grafico devem usar volatilidade historica diaria de 252 retornos logaritmicos e serem projetadas a partir do fechamento diario anterior, nunca a partir da volatilidade intraday. Esse grafico deve ser leve, com cache curto, e servir como leitura rapida antes do grafico proprietario Lightweight.
- **Grafico proprietario no Terminal de Trading:** A pagina `Terminal de Trading` deve conter uma copia do grafico proprietario Lightweight Charts com `signal_mode='reversal'`, titulo `Motor de reversao - copia` e `instance_id` proprio para nao conflitar com a pagina `Graficos Avancados`.
- **Performance do Terminal de Trading:** O primeiro carregamento do Terminal de Trading deve evitar chamadas externas pesadas. Top Movers Brasil e graficos comparativos devem carregar sob demanda por botao, com cache mais longo e timeouts curtos, para preservar abertura rapida do terminal e reduzir travamentos no Streamlit Cloud.
- **Regime de Juros RTD:** O card `Regime de Juros` do Terminal de Trading le `app_state.regime_juros` no Supabase. Para manter o dado vivo, o PC local deve rodar a tarefa do Windows `TTS Regime Juros Sync`, instalada por `scripts/install_regime_juros_task.ps1`, de segunda a sexta das 09:00 as 17:00. O loop `scripts/sync_regime_juros_loop.ps1` executa `execution/sync_regime_juros.py` a cada 30s, lendo o Excel aberto com RTD via COM e gravando em Supabase. O loop deve tolerar disparo alguns segundos antes da janela e aguardar a abertura, pois o Agendador do Windows pode iniciar no limite de 09:00. O leitor COM deve tentar `GetActiveObject` e, se `Workbooks` falhar, recuperar por `Dispatch("Excel.Application")` antes de cair para Excel salvo. Log em `.tmp/regime_juros_sync.log`.
- **Auditoria de dados de mercado:** Variacao diaria deve usar fechamento da sessao anterior como base e `high/low` exibidos no painel global devem representar a sessao mais recente, nao a janela inteira de coleta. Ao parsear APIs JSON como Brapi, nao usar operador `or` para campos numericos que podem ser zero; usar checagem explicita de `None`.
- **Painel de cotacoes:** `execution/fetch_global_markets.py` deve preencher `change_5m` diretamente no payload `mercados_globais`, usando o candle mais proximo de 5 minutos atras. A barra lateral deve preferir esse campo para exibir aceleracao, usando historico de sessao apenas como fallback. Jobs locais/server-side devem conseguir ler `.streamlit/secrets.toml` para sincronizar `mercados_globais` no Supabase quando as variaveis de ambiente nao estiverem exportadas. O coletor deve usar roteamento por ativo: Yahoo Finance como base em lote; se o candle vier atrasado ou vazio, tentar Brapi, Twelve Data e Alpha Vantage conforme o tipo de ativo/mapeamento disponivel, escolhendo a cotacao valida com menor `age_seconds`. O workflow `.github/workflows/update_market_data.yml` deve rodar 24/7 a cada 5 minutos para reduzir atraso do painel. O payload deve incluir `source`, `source_symbol`, `source_timestamp`, `age_seconds` e resumo `metadata.sources` para auditoria de atraso por API.
